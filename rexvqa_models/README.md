# rexvqa-models

Local Python package used by the ReXVQA experiments.

It contains model backend wrappers, prompt builders, image utilities, result evaluators, LoRA merge helpers, quota dataset builders, and the SDFT training utilities used by the repository-level scripts.

Install it through the workspace root:

```bash
uv sync
```
